#include <bits/stdc++.h>

using namespace std;

int main()
{
    queue<int> line;
    line.push(4);
    line.push(7);
    line.push(9);

    int firstElement = line.front();
    int lastElement = line.back();

    int length = line.size();

    return 0;
}